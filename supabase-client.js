(function(){
'use strict';
const SUPABASE_URL='https://qpvtygafwobaxltdntdg.supabase.co';
const SUPABASE_KEY='sb_publishable_cjJpoVZoghi9x1bzI5H2ng_Zh_GNxx5';
const APP_URL='https://detector-producto-ganador.vercel.app';
const STORE_KEY='dpg_saved_research_v39',PENDING_KEY='dpg_pending_research_v39',INTENT_KEY='dpg_save_intent_v40',NAME_KEY='dpg_pending_name_v39',VISITOR_KEY='dpg_visitor_id_v39';
let sb=null,session=null,lastResearch=null,authModal=null,promptShownForResearchId=null,pollTimer=null;
function esc(v){return String(v==null?'':v).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));}
function loadScript(){return new Promise((resolve,reject)=>{if(window.supabase?.createClient)return resolve();const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2';s.onload=resolve;s.onerror=reject;document.head.appendChild(s);});}
function showStatus(message,type){const el=document.getElementById('status');if(!el)return;el.className='status show'+(type==='error'?' error':type==='success'?' success':'');el.textContent=message;}
function getVisitorId(){try{let id=localStorage.getItem(VISITOR_KEY);if(!id){id='v_'+Date.now().toString(36)+'_'+Math.random().toString(36).slice(2,10);localStorage.setItem(VISITOR_KEY,id);}return id;}catch{return 'anonymous';}}
function openAuthModal(mode='save'){if(!authModal)return;const title=document.getElementById('dpg-auth-title'),desc=document.getElementById('dpg-auth-desc'),msg=document.getElementById('dpg-auth-msg');if(title)title.textContent=mode==='save'?'🔐 Guarda esta investigación':'🔐 Guarda tus investigaciones';if(desc)desc.innerHTML='Puedes <b>descargar e imprimir</b> el informe sin registrarte. Si quieres conservarlo en la nube y abrirlo después desde cualquier dispositivo, crea tu acceso gratuito con nombre y correo.';if(msg&&!msg.textContent.startsWith('✅'))msg.textContent='';authModal.style.display='flex';}
function closeAuthModal(){if(authModal)authModal.style.display='none';}
function getPendingName(){try{return localStorage.getItem(NAME_KEY)||'';}catch{return '';}}
function setPendingName(name){try{localStorage.setItem(NAME_KEY,name);}catch{}}
function setIntentId(id){try{if(id)localStorage.setItem(INTENT_KEY,id);else localStorage.removeItem(INTENT_KEY);}catch{}}
function getIntentId(){try{return localStorage.getItem(INTENT_KEY)||'';}catch{return '';}}
function clearIntentId(){setIntentId('');}
function loadPending(){if(lastResearch)return lastResearch;try{const raw=localStorage.getItem(PENDING_KEY);if(raw)lastResearch=JSON.parse(raw);}catch(e){console.warn('pending load',e);}return lastResearch;}
function clearPending(){try{localStorage.removeItem(PENDING_KEY);}catch{}lastResearch=null;}
function getSafeRedirect(intentId){return APP_URL+'/auth/confirm.html?intent='+encodeURIComponent(intentId);}
async function ensureProfile(){if(!session?.user)return false;const user=session.user,pendingName=getPendingName();const payload={id:user.id,full_name:pendingName||user.user_metadata?.full_name||user.email||'Usuario',email:user.email||null,updated_at:new Date().toISOString()};const {error}=await sb.from('profiles').upsert(payload,{onConflict:'id'});if(error){console.warn('profile',error);return false;}return true;}
async function event(name,extra={}){try{await sb.from('usage_events').insert({user_id:session?.user?.id||null,event_name:name,product_count:extra.product_count||null,metadata:Object.assign({visitor_id:getVisitorId()},extra)});}catch(e){console.warn('analytics',e);}}
async function saveCloud(item){if(!session?.user)return {ok:false,reason:'login'};const report=item.report||item.result||item,title=item.title||report?.overallWinner?.productName||'Investigación',productCount=Number(item.productCount||report?.products?.length||1),researchId=item.researchId||report?.researchId||('LOCAL-'+Date.now());const payload={user_id:session.user.id,research_id:researchId,title,product_count:Math.max(1,Math.min(5,productCount)),winner_product:report?.overallWinner?.productName||item.winner_product||null,report};const {error}=await sb.from('research_reports').upsert(payload,{onConflict:'user_id,research_id'});if(error){console.warn('saveCloud',error);return {ok:false,reason:'database',error};}await event('report_saved',{product_count:productCount,research_id:researchId});return {ok:true,researchId};}
async function createSaveIntent(email,research){const {data,error}=await sb.rpc('create_save_intent',{p_email:email,p_research:research});if(error||!data){console.warn('create_save_intent',error);return {ok:false,error};}return {ok:true,id:data};}
async function getIntentStatus(id){const {data,error}=await sb.rpc('get_save_intent_status',{p_intent_id:id});if(error){console.warn('get_save_intent_status',error);return {ok:false,error};}return data||{ok:false};}
async function completeIntent(id){const {data,error}=await sb.rpc('complete_save_intent',{p_intent_id:id});if(error){console.warn('complete_save_intent',error);return {ok:false,error};}return data||{ok:false};}
async function finishPendingIfConfirmed(){const id=getIntentId();if(!id)return false;const st=await getIntentStatus(id);if(!st.ok)return false;if(st.status==='confirmed'||st.status==='completed'){const done=st.status==='completed'?{ok:true}:await completeIntent(id);if(done.ok){clearPending();clearIntentId();try{localStorage.removeItem(NAME_KEY);}catch{}await event('report_saved_cross_device',{research_id:done.research_id||null});showStatus('✅ Investigación guardada correctamente en tu cuenta. La confirmación se realizó desde cualquier dispositivo.','success');const h=document.getElementById('showHistory');if(h)h.click();return true;}}return false;}
function startIntentPolling(id){if(!id)return;if(pollTimer)clearInterval(pollTimer);let attempts=0;pollTimer=setInterval(async()=>{attempts++;const done=await finishPendingIfConfirmed();if(done||attempts>=200){clearInterval(pollTimer);pollTimer=null;}},3000);}
async function syncCloudHistory(){if(!session?.user)return;const {data,error}=await sb.from('research_reports').select('*').order('created_at',{ascending:false}).limit(50);if(error){console.warn('history',error);return;}let local=[];try{local=JSON.parse(localStorage.getItem(STORE_KEY)||'[]');if(!Array.isArray(local))local=[];}catch{}const map=new Map(local.map(x=>[x.researchId||x.id,x]));(data||[]).forEach(row=>map.set(row.research_id,{id:row.id,researchId:row.research_id,title:row.title,productCount:row.product_count,winner:row.winner_product,report:row.report,createdAt:row.created_at,savedAt:row.created_at}));try{localStorage.setItem(STORE_KEY,JSON.stringify([...map.values()].sort((a,b)=>new Date(b.createdAt||b.savedAt||0)-new Date(a.createdAt||a.savedAt||0)).slice(0,50)));}catch{}}
async function autoSavePending(){if(getIntentId()){await finishPendingIfConfirmed();return;}const pending=loadPending();if(!session?.user||!pending)return;const result=await saveCloud({researchId:pending.researchId,title:pending.overallWinner?.productName||'Investigación',productCount:pending.products?.length||1,report:pending});if(result.ok){clearPending();showStatus('✅ Investigación guardada correctamente en la nube y asociada a tu cuenta.','success');}}
function showPostAnalysisSavePrompt(data){if(!data||!data.products?.length)return;const rid=data.researchId||data.overallWinner?.productName||String(Date.now());if(promptShownForResearchId===rid||session)return;promptShownForResearchId=rid;try{localStorage.setItem(PENDING_KEY,JSON.stringify(data));}catch{}setTimeout(()=>{if(!session)openAuthModal('save');},700);}
function addAccountUI(){const modal=document.createElement('div');modal.id='dpg-auth-modal';modal.style.cssText='display:none;position:fixed;inset:0;background:#0008;z-index:10000;align-items:center;justify-content:center;padding:20px';modal.innerHTML='<div style="max-width:430px;width:100%;background:white;border-radius:16px;padding:22px;box-shadow:0 20px 60px #0004"><h2 id="dpg-auth-title" style="margin-top:0">🔐 Guarda esta investigación</h2><p id="dpg-auth-desc" style="color:#667085;font-size:13px;line-height:1.5">Puedes <b>descargar e imprimir</b> el informe sin registrarte. Si quieres conservarlo en la nube y abrirlo después desde cualquier dispositivo, crea tu acceso gratuito con nombre y correo.</p><label style="display:block;font-size:12px;font-weight:700;margin:10px 0 5px">Nombre</label><input id="dpg-name" style="width:100%;box-sizing:border-box;padding:11px;border:1px solid #d7dbe5;border-radius:9px" placeholder="Tu nombre"><label style="display:block;font-size:12px;font-weight:700;margin:10px 0 5px">Correo</label><input id="dpg-email" type="email" style="width:100%;box-sizing:border-box;padding:11px;border:1px solid #d7dbe5;border-radius:9px" placeholder="tu@email.com"><div id="dpg-auth-msg" style="font-size:12px;margin:10px 0;line-height:1.45"></div><div style="display:flex;gap:8px;justify-content:flex-end"><button id="dpg-auth-close" style="border:0;border-radius:9px;padding:10px 14px;background:#eeeaff;color:#4937c4;font-weight:700">Ahora no</button><button id="dpg-auth-send" style="border:0;border-radius:9px;padding:10px 14px;background:#5b45ea;color:white;font-weight:700">Guardar y continuar</button></div></div>';document.body.appendChild(modal);authModal=modal;const savedName=getPendingName();if(savedName)document.getElementById('dpg-name').value=savedName;document.getElementById('dpg-auth-close').onclick=()=>{closeAuthModal();};document.getElementById('dpg-auth-send').onclick=async()=>{const name=document.getElementById('dpg-name').value.trim(),email=document.getElementById('dpg-email').value.trim(),msg=document.getElementById('dpg-auth-msg'),sendBtn=document.getElementById('dpg-auth-send'),pending=loadPending();if(!name||!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email)){msg.textContent='Escribe un nombre y un correo válido.';msg.style.color='#b42318';return;}if(!pending){msg.textContent='No hay una investigación activa para guardar.';msg.style.color='#b42318';return;}setPendingName(name);sendBtn.disabled=true;sendBtn.textContent='Preparando...';msg.textContent='Preparando el guardado seguro...';msg.style.color='#667085';const intent=await createSaveIntent(email,pending);if(!intent.ok){sendBtn.disabled=false;sendBtn.textContent='Guardar y continuar';msg.textContent='❌ No pudimos preparar el guardado. Intenta nuevamente.';msg.style.color='#b42318';return;}setIntentId(intent.id);const {error}=await sb.auth.signInWithOtp({email,options:{emailRedirectTo:getSafeRedirect(intent.id),shouldCreateUser:true,data:{full_name:name}}});if(error){sendBtn.disabled=false;sendBtn.textContent='Guardar y continuar';const rateLimited=/rate limit|too many|429/i.test(error.message||'');msg.textContent=rateLimited?'⚠️ El correo de acceso está temporalmente limitado. Tu investigación sigue disponible y no se ha perdido.':'❌ No pudimos enviar el enlace: '+error.message;msg.style.color='#b42318';if(rateLimited)setTimeout(()=>{closeAuthModal();showStatus('⚠️ El correo de acceso está temporalmente limitado. La investigación no se perdió; puedes descargarla ahora y volver a Guardar más tarde.','error');},3200);return;}msg.innerHTML='✅ Enlace enviado. Revisa tu correo y ábrelo para validar tu acceso. La investigación quedará asociada a este proceso y se guardará en el dispositivo donde la realizaste.';msg.style.color='#087443';sendBtn.disabled=true;startIntentPolling(intent.id);setTimeout(()=>{closeAuthModal();showStatus('✅ Enlace enviado. Revisa tu correo para validar tu acceso. La investigación quedó pendiente y se guardará automáticamente cuando confirmes.','success');},1800);};}
function patchAnalytics(){const originalFetch=window.fetch.bind(window);window.fetch=async function(input,init){const url=typeof input==='string'?input:input?.url||'',isResearch=String(url).includes('/api/research');if(!isResearch)return originalFetch(input,init);let body={};try{body=JSON.parse(init?.body||'{}');}catch{}await event('analysis_started',{product_count:body?.products?.length||null});const response=await originalFetch(input,init);try{const clone=response.clone(),data=await clone.json();if(response.ok){lastResearch=data;window.__DPG_LAST_RESEARCH__=data;try{localStorage.setItem(PENDING_KEY,JSON.stringify(data));}catch{}await event('analysis_completed',{product_count:data?.products?.length||null,research_id:data?.researchId||null});showPostAnalysisSavePrompt(data);}else await event('analysis_failed',{status:response.status,error:data?.error||'unknown'});}catch{}return response;};const demo=document.getElementById('demo');if(demo)demo.addEventListener('click',()=>event('demo_clicked',{product_count:5}));const history=document.getElementById('showHistory');if(history)history.addEventListener('click',()=>event('history_opened'));const newBtn=document.getElementById('newResearch');if(newBtn)newBtn.addEventListener('click',()=>{clearPending();clearIntentId();event('new_research');});document.addEventListener('click',async e=>{const b=e.target.closest('button');if(!b)return;const text=(b.textContent||'').toUpperCase();if(text.includes('GUARDAR')||text.includes('SAVE')){if(b.id==='dpg-auth-send'||b.id==='dpg-auth-close')return;if(!session){openAuthModal('save');event('save_requires_login');return;}const pending=loadPending();if(pending){const result=await saveCloud({researchId:pending.researchId,title:pending.overallWinner?.productName||'Investigación',productCount:pending.products?.length||1,report:pending});if(result.ok){clearPending();clearIntentId();showStatus('✅ Investigación guardada correctamente en la nube.','success');}else showStatus('❌ No se pudo guardar la investigación. Revisa la conexión con Supabase.','error');}else showStatus('⚠️ No hay una investigación activa para guardar.','error');}if(text.includes('DESCARGAR'))event('report_downloaded',{product_count:lastResearch?.products?.length||null});if(text.includes('EMAIL')||text.includes('CORREO'))event('email_clicked',{product_count:lastResearch?.products?.length||null});if(text.includes('ELIMINAR'))event('report_deleted');});}
async function init(){await loadScript();sb=window.supabase.createClient(SUPABASE_URL,SUPABASE_KEY,{auth:{autoRefreshToken:true,persistSession:true,detectSessionInUrl:true}});const r=await sb.auth.getSession();session=r.data.session||null;sb.auth.onAuthStateChange((_e,s)=>{session=s||null;if(session)setTimeout(async()=>{await ensureProfile();await syncCloudHistory();},150);});addAccountUI();patchAnalytics();event('app_opened',{page:location.pathname});await finishPendingIfConfirmed();if(getIntentId())startIntentPolling(getIntentId());if(session){await ensureProfile();await syncCloudHistory();await autoSavePending();}}
init().catch(e=>console.warn('Supabase init failed',e));
})();
